現状の ros_inference_advanced.py の選定はかなり単純で、
- 物体点群を受信
- 外れ値除去
- GraspGen 推論
- scene 点群があれば衝突候補を除外
- 残った候補の中で confidence 最大を選ぶ
**最低限の hard filter + confidence 最大**は実装済みです。

## 結論
候補選定は次の方針です。
- まず confidence だけで決めない
- ただし confidence は主スコアとして残す
- 干渉回避は hard filter にする
- 長手方向の重心に近いは soft prior にする
- 把持面に対して垂直は hard rule にしない
一番危ないのは最後です。
**把持面に対して垂直**が常に正しいわけではありません。平行 2 指では局所面法線に対してまっすぐ入るのが良い場合もありますが、物体の形状、支持面、周囲障害物、手先可動域で外れるケースがかなりあります。なので、これを強いルールにすると逆に落ちます。

## 推奨する選定ロジック
実装しやすさと効果のバランスで言うと、3 段構成がよいです。
### 1. Hard filter
ここは落としてよい候補を確実に落とします。
- scene 点群との衝突がある
- テーブル面に近すぎる
- 把持位置が物体点群の端すぎる
- 手先アプローチ方向に十分な空間がない
- TF に変換したときに HSR 手先で明らかに無理な姿勢
この段階は「失敗しやすい候補を消す」役です。
filter_colliding_grasps はもうあるので、その延長で support plane と approach clearance を足すのが良いです。

### 2. Soft scoring
残った候補に点数を付けて順位を付けます. おすすめはこうです。
total_score
= w_conf * model_confidence
+ w_center * centrality_score
+ w_clear * approach_clearance_score
+ w_stable * support_margin_score
+ w_align * geometry_alignment_score

ここで意味は次です。
- model_confidence
GraspGen 自体の予測信頼度。これは主成分として使う。
- centrality_score
物体長手方向の中心に近いほど高得点。
- approach_clearance_score
アプローチ方向に余裕があるほど高得点。
- support_margin_score
テーブルや周囲物体から十分離れているほど高得点。
- geometry_alignment_score
局所形状に対して無理のない把持向きほど高得点。

### 3. 最終 1 本を TF 出力
最終的には 1 本だけ TF を出すなら、上位 3-5 候補を保持しておいて、その中で motion planning 側が通りやすいものを使う構成もよいです。
もし grasp node 側だけで 1 本に絞るなら、上の総合スコア最大でよいです。

## その他アイデアの評価

### 干渉可能性が小さい
これは正しいです。しかも soft ではなく hard filter 寄りです。
テーブル・背景・周辺物体との衝突可能性が高い候補は最初に消すべきです。

### 長手方向の重心位置に近い
これはかなり良いです。ただし hard rule にはしない方がよいです。
理由は、細長い箱やボトルでは中央が安定しやすい一方で、ハンドル付き物体や非対称物体では中心が最適とは限らないからです。
なので「中央に近いほど少し加点」くらいがちょうどよいです。
実装としては、物体点群の PCA を取って第一主成分上に射影し、候補位置がその軸の中心に近いほど高得点にすればよいです。

### 把持面に対して垂直
これは「場合による」です。
強く入れすぎると危険です。
平行 2 指で重要なのは、雑に言うと、
- 指が閉じる方向で物体を挟みやすいか
- アプローチ方向に空間があるか
- 接触部の局所形状が安定か
であって、「面に垂直」が本質ではありません。
例えばこうです。
- 箱の側面把持なら、局所面に対してほぼ垂直な接近は自然
- でも寝た円柱やマグカップのような形では、面法線基準だけだと不安定
- テーブル上物体では、上から少し斜めに入る方が安全な場合もある
なので「面に垂直か」を見るなら、加点項目の 1 つに留めるべきです。
## 実装上おすすめの簡易版
最初から複雑にしないなら、まずこれで十分です。
### Hard filter
- GraspGen confidence が閾値未満
- scene 点群との衝突あり
- テーブル面から近すぎる
- 物体点群の境界から近すぎる
### Ranking
- 0.5 * confidence
- 0.2 * 長手方向中心への近さ
- 0.2 * アプローチ方向クリアランス
- 0.1 * 局所面法線との整合性
このくらいが実装しやすく、壊れにくいです。
## 面法線との整合性はどう見るか
「垂直であることが良いか？」に対しては、次の形が実務的です。
- 把持点近傍の局所点群で法線を推定
- Grasp のアプローチ方向と法線の角度を見る
- 角度が極端すぎる候補だけ減点する
- ただし最良条件として固定しない
つまり
- 良さそうなら少し加点
- 明らかに不自然なら減点
- 必須条件にはしない

## いまのコードへの落とし込み
現状の scripts/ros_inference_advanced.py だと、final_grasps を作ったあとに
best_idx = np.argmax(final_conf)
で決めています。
ここをbest_idx = np.argmax(total_score)に変えるだけで、構造は保てます。
つまり、追加すべきなのは「final_grasps ごとの再ランキング関数」です。
設計としては launch に次のような重みを持たせるのがよいです。
score_weight_confidence
score_weight_centrality
score_weight_clearance
score_weight_surface_alignment
table_clearance_threshold
collision_threshold

こうしておけば、今の「ハードコーディング回避」の方針と一致します。
## 一番現実的な方針
まずはこう進めるのがよいです。
1. 既存の collision filter + confidence最大 をベースにする
2. そこへ 長手方向中心への近さ を soft score で追加する
3. テーブル面クリアランス を hard filter で追加する
4. 面法線に対して垂直 は hard rule にせず、弱い加点に留める
5. まずログを取って、選ばれた候補が直感とズレるケースを集める
この順なら、過剰設計になりません。
必要なら次に、
- ros_inference_advanced.py に入れる再ランキング関数の具体案
- PCA 中心性スコアの計算式
- テーブル干渉・アプローチクリアランスの簡易実装
まで落として提案できます。
